@startuml
(*top) -->"Periodic Listen"

if "Message received ?" then
  -->"Periodic Listen"
else
  [Yes]if "" then
    -right->[Answer]"Forward answer to recipient"
    -->"Send Ack"
    -left->"Periodic Listen"
  else
    [Request]if "" then
      -->"Handle Req"
      -up->"Periodic Listen"
    else
      -->"Create proc Req"
      -up->"Periodic Listen"
    endif
  endif
endif

@enduml
