@startuml
(*top) -->"Periodic Listen"

if "Message received ?" then
  -left->[No]"Do other things"
  -->"Periodic Listen"
else
  [Yes]if "" then
    -right->[Answer]"Send Ack"
    -->"Periodic Listen"
  else
    [Request]if "" then
      -->"Handle Req"
      -up->"Periodic Listen"
    else
      -->"Create proc Req"
      -up->"Periodic Listen"
    endif
  endif
endif

@enduml
